name = "talk-to-yourself"
main = "worker.js"
compatibility_date = "2024-01-01"

kv_namespaces = [
  { binding = "STORE", id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID" }
]

# Store the auth secret securely — never put it in this file:
#   npx wrangler secret put AUTH_SECRET
